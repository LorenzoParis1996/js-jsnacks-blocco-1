/*sbagliato metodo applicato, da rifare snack*/


const numOne = parseInt(prompt('inserisci un numero'));
const numTwo = parseInt(prompt('inserisci un numero'));
const numThree = parseInt(prompt('inserisci un numero'));
const numFour = parseInt(prompt('inserisci un numero'));
const numFive = parseInt(prompt('inserisci un numero'));
const numSix = parseInt(prompt('inserisci un numero'));
const isOdd = number => number % 2 !== 0;

let numList = [numOne, numTwo, numThree, numFour, numFive, numSix];
numList = [];

if (numOne, numTwo, numThree, numFour, numFive, numSix === isOdd) {
    console.log(numList.push(numOne, numTwo, numThree, numFour, numFive, numSix));
}